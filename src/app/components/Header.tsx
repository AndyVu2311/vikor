import { navLinks } from '../../../constants';
import NavBar from './NavBar';

export const Header = () => {
  return (
    <header className='relative w-full'>
      <NavBar />
    </header>
  );
};
